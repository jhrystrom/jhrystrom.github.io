[project]
name = "jhrystrom-site"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
    "markdown>=3.10.3",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "mdformat>=1.0.0",
    "mdformat-frontmatter>=2.1.2",
    "pre-commit>=4.4.0",
    "ruff>=0.16.6",
]
